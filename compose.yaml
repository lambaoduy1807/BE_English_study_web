name: english-study 
version: '3.8'

services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: English-backend
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env